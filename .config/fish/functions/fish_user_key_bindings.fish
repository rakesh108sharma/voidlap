function fish_user_key_bindings
  # dmenu
  bind \cr dmenu_select_history # Bind for peco select history to Ctrl+R

  # prevent terminal from closing when typing Ctrl-D (EOF)
#  bind \cd delete-char
end
